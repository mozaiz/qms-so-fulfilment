# QMS — SO Fulfilment
# Runtime dependencies only. Keep this list short: every entry is something a
# store box has to download on install day.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
# Pydantic evaluates model type hints at runtime, and `X | None` is not valid to
# that evaluator on Python 3.9 — which is exactly what macOS Command Line Tools
# ship. Installing this lets Pydantic backport it. Belt and braces: the models
# above are written with Optional[] anyway, so this only catches a future slip.
eval_type_backport>=0.2 ; python_version < "3.10"
# Only needed for the setup page QR code (so staff can scan the address instead
# of typing an IP). The app degrades gracefully if Pillow is missing.
qrcode>=7.4
pillow>=10.0
# Generates the local certificate authority and server certificate that let a
# phone camera work (a browser only gives a camera on a secure context, and a
# LAN IP over plain HTTP is not one). Only needed for HTTPS, but it is a small
# single wheel and leaving it out means the phone-scanning path is missing on a
# fresh install.
cryptography>=42.0
